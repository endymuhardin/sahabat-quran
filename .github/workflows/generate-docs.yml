name: Generate Complete System Documentation and Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  generate-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: sahabat_quran_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Cache Maven dependencies
      uses: actions/cache@v4
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2

    - name: Install Playwright dependencies and pandoc
      run: |
        sudo apt-get update
        sudo apt-get install -y xvfb libwoff1 libvpx9 libevent-2.1-7t64 libopus0 \
          libgstreamer-plugins-base1.0-0 libgstreamer-gl1.0-0 \
          libgstreamer-plugins-bad1.0-0 libflite1 libavif16 libharfbuzz-icu0 \
          libsecret-1-0 libhyphen0 libmanette-0.2-0 libgles2 gstreamer1.0-libav \
          pandoc python3

    - name: Install Playwright
      run: |
        ./mvnw clean compile
        npx playwright install chromium

    - name: Set up database
      env:
        SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/sahabat_quran_test
        SPRING_DATASOURCE_USERNAME: postgres
        SPRING_DATASOURCE_PASSWORD: postgres
      run: |
        ./mvnw flyway:migrate -Dflyway.url=jdbc:postgresql://localhost:5432/sahabat_quran_test -Dflyway.user=postgres -Dflyway.password=postgres

    - name: Generate complete system documentation
      env:
        SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/sahabat_quran_test
        SPRING_DATASOURCE_USERNAME: postgres
        SPRING_DATASOURCE_PASSWORD: postgres
        PLAYWRIGHT_HEADLESS: true
      run: |
        chmod +x ./generate-user-manual.sh
        xvfb-run -a ./generate-user-manual.sh generate --headless

    - name: Setup Pages
      uses: actions/configure-pages@v5

    - name: Prepare documentation for Pages
      run: |
        mkdir -p _site
        cp -r target/documentation/* _site/
        
        # Copy CSS and template files to _site
        cp scripts/documentation-template.css _site/
        cp scripts/pandoc-template.html _site/
        
        # Convert markdown files to HTML with custom CSS for modern, wide layout
        find _site -name "*.md" -type f | while read -r file; do
          if command -v pandoc >/dev/null 2>&1; then
            html_file="${file%.md}.html"
            # Get relative path to CSS file (for subdirectories)
            dir_depth=$(echo "${file#_site/}" | tr -cd '/' | wc -c)
            css_path=""
            if [ $dir_depth -gt 0 ]; then
              for i in $(seq 1 $dir_depth); do
                css_path="../$css_path"
              done
            fi
            css_path="${css_path}documentation-template.css"
            
            # Use custom template and CSS for modern, wide layout
            pandoc "$file" -o "$html_file" \
              --standalone \
              --template="_site/pandoc-template.html" \
              --metadata title="Sahabat Quran Documentation" \
              --css="$css_path" \
              --toc \
              --toc-depth=3
          fi
        done
        
        # Generate individual index files for each documentation section
        chmod +x scripts/generate-pages-index.sh
        ./scripts/generate-pages-index.sh
        
        # Create main index.html that lists available documentation
        current_date=$(date '+%d %B %Y %H:%M WIB')
        cat > _site/index.html << EOF
        <!DOCTYPE html>
        <html lang="id">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>Sahabat Quran - Dokumentasi Sistem</title>
            <style>
                body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; margin: 0; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh; }
                .header { background: rgba(255,255,255,0.1); backdrop-filter: blur(10px); padding: 20px 0; margin-bottom: 40px; }
                .container { max-width: 1000px; margin: 0 auto; padding: 0 20px; }
                .title { color: white; text-align: center; font-size: 2.5em; margin: 0; text-shadow: 2px 2px 4px rgba(0,0,0,0.3); }
                .subtitle { color: rgba(255,255,255,0.9); text-align: center; font-size: 1.2em; margin: 10px 0 0 0; }
                .main-content { background: white; border-radius: 20px; padding: 40px; box-shadow: 0 20px 40px rgba(0,0,0,0.1); }
                .section-title { color: #2c3e50; font-size: 1.8em; margin-bottom: 20px; display: flex; align-items: center; gap: 10px; }
                .guide-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; margin: 20px 0; }
                .guide-card { background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%); padding: 25px; border-radius: 15px; border: none; transition: all 0.3s ease; position: relative; overflow: hidden; }
                .guide-card:hover { transform: translateY(-5px); box-shadow: 0 15px 30px rgba(0,0,0,0.2); }
                .guide-card::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 4px; background: linear-gradient(90deg, #667eea, #764ba2); }
                .guide-card h3 { margin: 0 0 10px 0; color: #2c3e50; font-size: 1.4em; }
                .guide-card a { text-decoration: none; color: inherit; }
                .guide-card a:hover { color: #667eea; }
                .guide-description { color: #5a6c7d; line-height: 1.6; }
                .main-docs { margin-bottom: 40px; }
                .workflow-docs { }
                .stats { background: #f8f9fa; padding: 20px; border-radius: 10px; margin: 30px 0; text-align: center; }
                .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 20px; }
                .stat-item { }
                .stat-number { font-size: 2em; font-weight: bold; color: #667eea; margin: 0; }
                .stat-label { color: #6c757d; margin: 5px 0 0 0; }
                .footer { text-align: center; color: #6c757d; margin-top: 40px; padding-top: 20px; border-top: 1px solid #e9ecef; }
            </style>
        </head>
        <body>
            <div class="header">
                <div class="container">
                    <h1 class="title">📚 Sahabat Quran</h1>
                    <p class="subtitle">Dokumentasi Sistem Manajemen Akademik</p>
                </div>
            </div>
            
            <div class="container">
                <div class="main-content">
                    <div class="main-docs">
                        <h2 class="section-title">📋 Dokumentasi Utama</h2>
                        <div class="guide-grid">
                            <div class="guide-card">
                                <a href="PANDUAN_SISTEM_LENGKAP.html">
                                    <h3>🌟 Panduan Sistem Lengkap</h3>
                                    <p class="guide-description">Dokumentasi master yang mencakup semua workflow sistem: pendaftaran siswa dan persiapan semester dalam satu panduan terpadu.</p>
                                </a>
                            </div>
                            <div class="guide-card">
                                <a href="SUMMARY.html">
                                    <h3>📊 Ringkasan Dokumentasi</h3>
                                    <p class="guide-description">Index lengkap semua dokumentasi, struktur file, dan panduan penggunaan sistem dokumentasi.</p>
                                </a>
                            </div>
                        </div>
                    </div>

                    <div class="workflow-docs">
                        <h2 class="section-title">🔄 Dokumentasi Workflow</h2>
                        <div class="guide-grid">
                            <div class="guide-card">
                                <a href="PANDUAN_PENDAFTARAN.html">
                                    <h3>🎓 Workflow Pendaftaran Siswa</h3>
                                    <p class="guide-description">Panduan lengkap 3 tahap: Pendaftaran Siswa → Penugasan Admin → Evaluasi Guru. Termasuk screenshot dan video tutorial.</p>
                                </a>
                            </div>
                            <div class="guide-card">
                                <a href="PANDUAN_PERSIAPAN_SEMESTER.html">
                                    <h3>📅 Workflow Persiapan Semester</h3>
                                    <p class="guide-description">Panduan lengkap 6 fase: Assessment → Distribution → Availability → Assignment → Generation → Go-Live.</p>
                                </a>
                            </div>
                        </div>
                    </div>

                    <div class="stats">
                        <h3 style="margin-top: 0; color: #495057;">📈 Statistik Dokumentasi</h3>
                        <div class="stats-grid">
                            <div class="stat-item">
                                <p class="stat-number">2</p>
                                <p class="stat-label">Workflow Utama</p>
                            </div>
                            <div class="stat-item">
                                <p class="stat-number">9</p>
                                <p class="stat-label">Total Fase/Tahap</p>
                            </div>
                            <div class="stat-item">
                                <p class="stat-number">100+</p>
                                <p class="stat-label">Screenshots HD</p>
                            </div>
                            <div class="stat-item">
                                <p class="stat-number">5+</p>
                                <p class="stat-label">Video Tutorial</p>
                            </div>
                        </div>
                    </div>

                    <div style="background: #e3f2fd; padding: 20px; border-radius: 10px; border-left: 5px solid #2196f3;">
                        <h4 style="margin-top: 0; color: #1565c0;">💡 Cara Menggunakan Dokumentasi</h4>
                        <ol style="color: #424242; line-height: 1.8;">
                            <li><strong>Mulai dengan Panduan Sistem Lengkap</strong> - Overview semua fitur</li>
                            <li><strong>Pilih workflow spesifik</strong> - Sesuai dengan peran Anda</li>
                            <li><strong>Ikuti step-by-step</strong> - Dengan bantuan screenshot</li>
                            <li><strong>Tonton video tutorial</strong> - Untuk demonstrasi visual</li>
                        </ol>
                    </div>

                    <div class="footer">
                        <p>🤖 Dokumentasi ini dihasilkan secara otomatis menggunakan Playwright testing framework</p>
                        <p>🔄 Terakhir diperbarui: $current_date</p>
                        <p>📧 Kontak: <a href="mailto:support@ysq.ac.id" style="color: #667eea;">support@ysq.ac.id</a></p>
                    </div>
                </div>
            </div>
        </body>
        </html>
        EOF

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '_site'

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4